需求
1 需要图片文件上传，下载功能
2 文件存储在本地目录，目录是/opt/app/img
3 需要有表存储文件名称与存放位置，表结构也生成给我
4 还有一张网站页面图片位置表，用于存放对应的图片信息
5 给前段提供一个根据网站图片位置编号，返回图片信息的接口
6 有多个网站，每个网站使用webId做区分，webId由人工指定，表结构留有这个字段。
7 用户打卡一次页面就算访问，然后每一秒发送一次请求，判断是否在页面停留。超过5分钟不计入停留时间
8 用户今日总访问量
9 天平均停留时间
10 本月累计访问 以及环比上月百分比
11 访问趋势折线图  小时、天、月
12 访问量数据存数据库。
